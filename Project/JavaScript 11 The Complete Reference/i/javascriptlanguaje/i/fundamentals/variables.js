"use strict";

let message = "Hello world";
let user = "John", age = 29, description = message;
const PI = Math.PI;

console.log(`Hi ${user}, your age is: ${age}, ${description}, number: ${PI.toFixed(2)}`);
